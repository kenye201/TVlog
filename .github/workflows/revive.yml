name: IPTV Daily Discovery

on:
  schedule:
    # 每天北京时间凌晨 04:00 运行 (UTC 20:00)
    - cron: '0 20 * * *'
  workflow_dispatch: # 关键：这行允许你在 GitHub Actions 页面手动点击运行

jobs:
  discovery:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: pip install requests

    - name: Run Discovery
      env:
        PYTHONUNBUFFERED: 1
      run: |
        # 运行挖矿脚本，将新活 IP 追加到 manual_fix.txt
        python md/discovery.py

    - name: Commit and Push
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        # 只提交 manual_fix.txt 的变更
        git add md/manual_fix.txt
        git diff --staged --quiet || (git commit -m "Discovery: 🌟 发现并追加新活 IP 到补丁库" && git push)
